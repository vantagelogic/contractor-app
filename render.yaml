services:
  - type: web
    name: contractor-api
    runtime: python
    rootDir: vantage_logic_api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
